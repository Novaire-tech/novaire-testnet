# Release Checklist — v0.2.0 (`b384401`)

- ✅ **Tests** — `tsc --noEmit` clean; frontend unit tests 14/14; scripts unit tests 7/7; contract test suite 260/260 (per `release-validation.md`, generated by whoever ran the RC — not re-run by this pass per instructions).
- ✅ **verify:testnet** — 4/5 scenarios PASS against live testnet contracts; Scenario E (yield-claim accrual) SKIPPED, documented scope limit, not a failure.
- ✅ **Build** — `next build` succeeds, 17 routes; Soroban `wasm32v1-none --release` build succeeds for all 10 contract crates (2 pre-existing warnings, no errors).
- ✅ **Contracts** — no contract source changed in this release commit; last contract change was `9356ca7` (already on master, not part of this diff).
- ⚠️ **Security** — TWAP-staleness and oracle-fallback fabrication fixed this release; underlying contract security hardening (SEC-03..SEC-14) shipped in prior commits, not re-audited here.
- ✅ **Deployment** — `assertRequiredAddresses` guard verified wired into both deploy scripts; no redeploy needed, contract IDs unchanged (see `deployment-summary.md`).
- ⚠️ **Documentation** — `CONTRIBUTING.md` added this release; `README.md` not reviewed for currency in this pass.
- ✅ **Release Notes** — `RELEASE_NOTES.md` generated.
- ✅ **Changelog** — `CHANGELOG.md` generated (first entry; project had none before).
- ❌ **Lint** — `npm run lint` fails (585 errors / 13,314 warnings), confirmed pre-existing baseline debt via `git stash` diff against base commit, not a regression. Not blocking per that finding, but not clean either — flag for follow-up.
- ❌ **Version bump** — `package.json`, `apps/web/package.json`, all 8 `Cargo.toml` still read `0.1.0`. No tag exists yet in this repo (`git tag -l` empty). Version files were not bumped as part of this release prep (out of scope: "do not modify code").

## Net

2 items marked ❌ are not test/build failures — they're process gaps (lint debt, unbumped version fields) that don't block tagging but should be tracked as follow-up, not silently ignored.
