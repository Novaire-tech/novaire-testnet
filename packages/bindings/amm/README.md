# amm bindings

Generated TS client for the `sidereal-amm` contract. Populated by
`contracts/scripts/deploy-testnet-resilient.sh` (via `stellar contract
bindings typescript`) as the last step of `npm run deploy`.
